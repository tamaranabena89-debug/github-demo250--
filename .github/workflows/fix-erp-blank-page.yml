name: Fix ERP blank page startup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: echo "Legacy startup patch disabled. ERP now uses the same-origin Supabase loader."