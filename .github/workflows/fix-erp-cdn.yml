name: ERP CDN Fallback

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy CDN workflow disabled
        run: echo "Legacy CDN fallback disabled. ERP uses the same-origin Supabase loader now." 
